pipeline {
    agent { label 'java11'}
    stages {
        stage ('terraform init') {
            steps {
                sh 'terraform init'
            }
        }
        stage ( 'terraform action') {
            steps {
                echo 'terraform action'
                sh 'terraform apply --auto-approve'
            }
        }
    }
}